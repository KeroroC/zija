<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zija.file.internal.persistence.StoredFileMapper">

    <!--
        清除过期附件：返回待物理删除的附件（回收站中已过保留期）。
    -->
    <select id="findExpired" resultType="com.zija.file.internal.persistence.StoredFileEntity">
        SELECT id, household_id, storage_key, original_filename, declared_media_type,
               detected_media_type, byte_size, sha256, created_at,
               mount_type, mount_id, name_normalized, deleted_at
        FROM stored_file
        WHERE deleted_at IS NOT NULL
          AND deleted_at &lt; #{before}
        ORDER BY deleted_at ASC
    </select>

</mapper>
