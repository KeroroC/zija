# 知家（zija）领域语言

本文件是知家项目唯一的领域术语表，只定义概念，不写实现细节。术语在阶段六 grill-with-docs 会话中随解随录。

## 库存核心

**物品（Item）**：
描述一种家庭用品「是什么」的资料记录，可复用。耐用品或消耗品。
_Avoid_: 商品、SKU、product

**批次（Lot）**：
描述某次购入或某件独立资产的实例。一个物品可有多个批次，每个批次独立到期与库存。
_Avoid_: 库存项、条目、batch（在代码注释外避免）

**库存位（Stock Position）**：
某批次在某位置的当前数量。数量不得为负。是库存模块的权威写模型。
_Avoid_: 余量、stock、库存（在指代具体某位时避免用「库存」一词）

**流水（Movement）**：
库存数量的一次不可变变更记录，是库存事实来源。类型为入库、领用、报损、盘点调整、移位、冲正。
_Avoid_: 记录、transaction、流水账（指代单条时避免）

## 阶段六：报表与数据交换

**查询端口（Query Port）**：
某模块公共 API 上暴露的只读查询方法，供外部模块调用以获取该模块的事实数据，不暴露原始实体或 Mapper。
_Avoid_: 查询服务、query service

**公开领域事件（Public Domain Event）**：
源模块发布的 record 类型跨模块事件，外部模块可订阅。字段**只能追加，不可重排或删除**；消费者与序列化器必须容忍新键缺失。
_Avoid_: 消息、message

**投影（Projection）**：
`reporting` 模块订阅他模块公开事件后，在事务提交后写入的自有扁平读模型表。报表与搜索查询的目标。允许在提交后短时间最终一致。
_Avoid_: 缓存、读副本、物化视图（实现未定时避免用「物化视图」）

**投影重建（Projection Rebuild）**：
从源数据重新填充投影表的过程，用于首次构建、事件丢失补齐或投影 schema 变更后。依赖源模块暴露的拉取/快照端口。

**导出（Export）**：
按当前筛选或全集把领域数据以 CSV 输出；用于数据携带，不是完整备份。阶段六只交付导出，不交付 CSV 导入。
_Avoid_: 备份、download

**全局搜索（Global Search）**：
跨物品名/品牌/标签/批次号/序列号/位置名的中文子串匹配查询，返回按实体类型分组的结果。不承诺拼音或语义搜索。
_Avoid_: 全文检索、搜索（单独「搜索」一词指代全局搜索时优先用「全局搜索」）