# 知家（zija）领域语言

本文件是知家项目唯一的领域术语表，只定义概念，不写实现细节。术语在阶段六 grill-with-docs 会话中随解随录。

## 附件

**附件（Attachment）**：
家庭保管的一份文档或图片。它自己携带挂载点，任一时刻恰好挂在家庭、物品或批次之一，可以改挂而不必重传。有一个可改的名字，初始来自上传时的原始文件名。同一挂载点下，未进入回收站的附件名字不可重复；撞名时必须改名才能完成上传、改名、改挂或恢复。删除后进入回收站，保留期满后物理删除。物品归档不冻结其上的附件。一份附件就是单个文件，不是压缩包；没有系统级种类。
_Avoid_: 文件（指业务文档时）、档案、文档、种类、说明书、小票（当作类型时）、压缩包、标题（没有第二套标题）

**挂载点（Mount Point）**：
附件当前所挂的位置，类型为家庭、物品或批次之一，任一时刻恰好一处。附件自身携带挂载点；家庭挂载点的挂载 ID 即家庭自身 ID。
_Avoid_: 所属、归属、位置（位置在库存域指库存位）

**改挂（Remount）**：
把附件从当前挂载点换挂到家庭、物品或批次之一，不重传内容。目标校验由目标所在模块入口负责；若被改挂的附件是某物品封面，原物品的封面指定被清除。UI 文案用「移到 X」，领域指称用「改挂」。
_Avoid_: 搬移、转存、移动（当术语指称时）

**家庭附件（Household Attachment）**：
挂载点为家庭、未挂在任何物品或批次上的附件，由家庭整体保管。无封面资格；改挂到物品或批次后按新挂载点获得相应资格。
_Avoid_: 公共附件、未挂载附件、孤儿附件（附件总是挂在家庭/物品/批次之一，不存在无挂载状态）

**回收站（Recycle Bin）**：
已删除、尚未物理清除的附件所处的状态。不出现在默认的家庭、物品、批次附件列表中，保留期内可以恢复，也可不等保留期由成员直接永久删除。
_Avoid_: 归档（归档是物品的长期状态，不是附件删除）

**保留期（Retention Period）**：
附件进入回收站之后、物理删除之前的时间。可配置。用户可主动永久删除，无需等待保留期届满。
_Avoid_: 归档期

**永久删除（Permanent Deletion）**：
把回收站中的附件立即物理清除的动作：数据卷对象与数据库行同时删除，跳过保留期，不可恢复，此后生成的备份不再包含该文件。任何成员可执行，与回收/恢复同级。执行后不发公开领域事件。
_Avoid_: 直接删除、彻底删除（口语可用，领域指称用「永久删除」）

**封面（Cover）**：
物品上某份合格图片附件被指定为「这件物品长什么样」，用于列表缩略图和详情头图。指定是从已有合格附件中选出（也可先新增再指定）；取消指定不等于移除附件。封面指定不进回收站：删除封面附件后该物品不再有封面，恢复后是普通附件。若封面附件被改挂到别处，该物品也不再有封面。同一物品至多一个封面；家庭附件、批次附件、HEIC 与文档都无资格。
_Avoid_: 封面图（当作与附件并列的类型时）、cover image（当作独立概念时）

## 库存核心

**物品（Item）**：
描述一种家庭用品「是什么」的资料记录，可复用。耐用品或消耗品。
_Avoid_: 商品、SKU、product

**批次（Lot）**：
描述某次购入或某件独立资产的实例。一个物品可有多个批次，每个批次独立到期与库存。
_Avoid_: 库存项、条目、batch（在代码注释外避免）

**库存位（Stock Position）**：
某批次在某位置的当前数量。数量不得为负。是库存模块的权威写模型。
_Avoid_: 余量、stock、库存（在指代具体某位时避免用「库存」一词）

**流水（Movement）**：
库存数量的一次不可变变更记录，是库存事实来源。类型为入库、领用、报损、盘点调整、移位、冲正。
_Avoid_: 记录、transaction、流水账（指代单条时避免）
> 报表侧只有「流水」一个概念。所谓「库存变化」不是独立概念，而是同一套流水的子视图——按时间/物品/位置或操作人等维度筛选流水。报表菜单只保留一个「流水」入口，位置与操作人都是它的筛选维度；移位这类有来源与目标的流水，位置筛选对来源或目标任一命中即出。不要为了不同的筛选组合再造并列页面或并列词条。

## 阶段六：报表与数据交换

**查询端口（Query Port）**：
某模块公共 API 上暴露的只读查询方法，供外部模块调用以获取该模块的事实数据，不暴露原始实体或 Mapper。
_Avoid_: 查询服务、query service

**公开领域事件（Public Domain Event）**：
源模块发布的 record 类型跨模块事件，外部模块可订阅。字段**只能追加，不可重排或删除**；消费者与序列化器必须容忍新键缺失。
_Avoid_: 消息、message

**投影（Projection）**：
`reporting` 模块订阅他模块公开事件后，在事务提交后写入的自有扁平读模型表。报表与搜索查询的目标。允许在提交后短时间最终一致。
_Avoid_: 缓存、读副本、物化视图（实现未定时避免用「物化视图」）

**投影重建（Projection Rebuild）**：
从源数据重新填充投影表的过程，用于首次构建、事件丢失补齐或投影 schema 变更后。依赖源模块暴露的拉取/快照端口。

**导出（Export）**：
按当前筛选或全集把领域数据以 CSV 输出；用于数据携带，不是完整备份。阶段六只交付导出，不交付 CSV 导入。
_Avoid_: 备份、download

**全局搜索（Global Search）**：
跨物品名/品牌/标签/批次号/序列号/位置名的中文子串匹配查询，返回按实体类型分组的结果。不承诺拼音或语义搜索。
_Avoid_: 全文检索、搜索（单独「搜索」一词指代全局搜索时优先用「全局搜索」）

## 提醒域

**提醒任务（Reminder Task）**：
对某批次临期（EXPIRY，按批次）或某物品低库存（LOW_STOCK，按物品）的一次待处理提醒记录，由 reconcile 过程依据当前规则与库存建立、刷新或自动关闭。
_Avoid_: 待办、todo、提醒（单独「提醒」一词指通知时避免与此混淆）

**任务状态（Task Status）**：
任务的生命周期位。OPEN 待处理、SNOOZED 已延后、DONE 已完成（含自动关闭）、IGNORED 已忽略。
_Avoid_: 任务类型（kind 才是类型）、状态机

**任务快照（Task Snapshot）**：
任务建立或刷新那一刻物化下来的数量与阈值（剩余数量、低库存阈值、提醒天数窗），是任务展示的依据。因每次库存变更事件都会触发重算，快照与最新库存的时间差约等于最后一次相关事件发生时刻；库存未变即快照仍然成立。物品名**不属于**快照——它是展示时从品目读到的活数据。
_Avoid_: 阈值快照、快照（在指代任务数据时避免泛用「快照」，以免与投影重建的「快照」冲突）

## 阶段七：发布加固

**备份（Backup）**：
在单一备份批次标识下捕获 PostgreSQL `pg_dump`、文件卷镜像、清单（含 schema 版本、应用版本、文件清单与每文件 SHA256、db 段校验和）的自包含目录。用于系统级恢复，与数据库处于同一备份策略。文件卷含尚未物理删除的附件（含回收站内）。
_Avoid_: 数据导出、CSV、快照、snapshot（快照在阶段六专指投影重建的源数据拉取）、封面文件卷

**备份批次标识（Backup Batch Id）**：
一份备份目录的唯一标识，形如 `backup_<id>_<timestamp>/`。is 备份的命名与恢复时的定位键。
_Avoid_: 备份名、backup name

**恢复（Restore）**：
向空环境（空 PostgreSQL 数据卷、空文件卷）还原一份备份，再由应用启动时 Flyway 校验/前向迁移，最后跑一致性检查。不是应用内动作，是宿主机运维动作。
_Avoid_: 还原、回滚、import

**应用版本（App Version）**：
知家发行版本的稳定标识，写入运行镜像、系统信息和备份清单。备份与恢复用它判断备份来源与恢复目标是否同一发行基线。
_Avoid_: 构建号、build 号